{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "cross-env NODE_ENV=test mocha --file test/setup.js --timeout 10000 \"test/**/*.test.js\"",
    "test:file": "cross-env NODE_ENV=test mocha --file test/setup.js --timeout 10000",
    "test:login": "cross-env NODE_ENV=test mocha --file test/setup.js --timeout 10000 test/auth/login.test.js",
    "test:register": "cross-env NODE_ENV=test mocha --file test/setup.js --timeout 10000 test/auth/register.test.js",
    "test:refresh": "cross-env NODE_ENV=test mocha --file test/setup.js --timeout 10000 test/auth/refresh.test.js",
    "test:logout": "cross-env NODE_ENV=test mocha --file test/setup.js --timeout 10000 test/auth/logout.test.js",
    "test:forgot-password": "cross-env NODE_ENV=test mocha --file test/setup.js --timeout 10000 test/auth/forgotPassword.test.js",
    "test:reset-password": "cross-env NODE_ENV=test mocha --file test/setup.js --timeout 10000 test/auth/resetPassword.test.js",
    "test:auth": "cross-env NODE_ENV=test mocha --file test/setup.js --timeout 10000 \"test/auth/*.test.js\"",
    "start": "node src/server.js",
    "dev": "nodemon --watch src --exec \"node src/server.js\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "body-parser": "^2.2.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "express-validator": "^7.2.1",
    "helmet": "^8.1.0",
    "html-to-text": "^9.0.5",
    "jsonwebtoken": "^9.0.2",
    "lodash": "^4.17.21",
    "mongoose": "^8.18.2",
    "multer": "^2.0.2",
    "nodemailer": "^7.0.9",
    "pino": "^9.13.1",
    "pino-http": "^10.5.0",
    "sanitize-html": "^2.17.0",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "chai": "^6.2.0",
    "chai-http": "^5.1.2",
    "cross-env": "^10.1.0",
    "mocha": "^11.7.4",
    "mongodb-memory-server": "^10.2.3",
    "nodemon": "^3.1.10",
    "pino-pretty": "^13.1.2",
    "sinon": "^21.0.0",
    "supertest": "^7.1.4"
  }
}
